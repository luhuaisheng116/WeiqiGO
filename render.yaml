services:
  - type: web
    name: go-mvp
    runtime: node
    plan: free
    rootDir: go-mvp
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: true
